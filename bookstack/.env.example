# BookStack Configuration
# Copy this to .env and configure

# Database
MYSQL_ROOT_PASSWORD=CHANGE_ME_STRONG_PASSWORD
MYSQL_PASSWORD=CHANGE_ME_BOOKSTACK_DB_PASSWORD

# Email (Optional - for notifications)
MAIL_DRIVER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_FROM=your-email@example.com
MAIL_FROM_NAME="BookStack mAIn"
MAIL_ENCRYPTION=tls
MAIL_USERNAME=your-email@example.com
MAIL_EMAIL_PASSWORD=your-app-password

# Storage
# Volumes are mounted to /srv/cargo/volumes/bookstack/
# Ensure directory exists and has proper permissions (PUID=1005, PGID=1005)
